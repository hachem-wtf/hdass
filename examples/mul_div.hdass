[entry: main]

// (7 * 6) / 5 = 8 with a value-expression multiply and a non-rax division,
// returned as the exit status.
const SYS_EXIT = 60

proc main
{
    rbx = 7
    rcx = 6
    rbx = rbx * rcx   // 42

    rcx = 5
    rbx /= rcx        // 8

    rdi = rbx
    rax = SYS_EXIT
    syscall
}
