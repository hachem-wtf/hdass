// Sums 5 + 4 + 3 + 2 + 1 = 15 with a countdown loop, returns it as the status.
const SYS_EXIT = 60

proc main
{
    rbx = 0      // running total
    rcx = 5      // counter

loop:
    rbx += rcx
    rcx -= 1
    if rcx != 0
        goto loop

    rdi = rbx
    rax = SYS_EXIT
    syscall
}
