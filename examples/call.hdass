[entry: main]

// Passes an argument through a proc parameter, which exits with that value.
const SYS_EXIT = 60

proc exit_with(code: rdi)
{
    rax = SYS_EXIT
    syscall
}

proc main
{
    exit_with(21)
}
